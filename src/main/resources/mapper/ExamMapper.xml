<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cetide.codeforge.mapper.ExamMapper">

    <!-- 你可以在这里添加更多的自定义查询 -->

    <select id="getExamByChapterId" resultType="com.cetide.codeforge.model.entity.exams.Exam">
        SELECT * FROM exam WHERE chapter_id = #{chapterId}
    </select>

</mapper>